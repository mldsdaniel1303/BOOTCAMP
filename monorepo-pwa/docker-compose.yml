version: '3.8'
services:
  api:
    build: ./apps/api
    ports:
      - "3000:3000"
    container_name: bootcamp-api
  web:
    build: ./apps/web
    ports:
      - "8080:80"
    container_name: bootcamp-web
    environment:
      - VITE_API_URL=http://api:3000
    depends_on:
      - api
